if(NOT DEFINED SKIP_DROGON)
	add_subdirectory(drogon)
else()
	add_library(drogon INTERFACE)
endif()

if(NOT DEFINED SKIP_WX)
	set(wxBUILD_SHARED OFF)
	set(wxBUILD_MONOLITHIC ON)
	add_subdirectory(wxWidgets)
else()
	add_library(wxmono INTERFACE)
	add_library(wx::mono ALIAS wxmono)
endif()